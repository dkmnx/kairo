run:
  timeout: 5m
  modules-download-mode: readonly

linter-settings:
  cyclop:
    max-complexity: 25
  errcheck:
    check-type-assertions: true
    check-blank: true
  exhaustive:
    default-signifies-exhaustive: true
  funlen:
    lines: 100
    statements: 50
  gocognit:
    min-complexity: 25
  goconst:
    min-len: 10
    min-occurrences: 10
  gocritic:
    enabled-tags:
      - diagnostic
      - performance
      - style
    disabled-checks:
      - exitAfterDefer
  gocyclo:
    max-complexity: 25
  gofmt:
    simplify: true
  goimports:
    local-prefixes: github.com/dkmnx/kairo
  gosimple:
    checks:
      - all
  govet:
    enable-all: true
  ineffassign:
    check-type-assertions: true
  lll:
    line-length: 120
  misspell:
    locale: US
  nakedret:
    max-func-lines: 100
  nestif:
    min-complexity: 15
  noctx:
    send-checks: true
    recv-checks: true
  staticcheck:
    checks:
      - all
      - -ST1005
  stylecheck:
    checks:
      - all
      - -ST1005
  unconvert:
    check-type-assertions: true
    check-decl-assign: true
  unparam:
    check-type-assertions: true
  unused:
    check-exported: true
    check-generated: false

linters:
  disable-all: true
  enable:
    - bodyclose
    - cyclop
    - errcheck
    - errorlint
    - exhaustive
    - funlen
    - gocognit
    - goconst
    - gocritic
    - gocyclo
    - gofmt
    - goimports
    - gosimple
    - govet
    - ineffassign
    - lll
    - misspell
    - nakedret
    - nestif
    - nlreturn
    - noctx
    - staticcheck
    - stylecheck
    - typecheck
    - unconvert
    - unparam
    - unused
    - whitespace

issues:
  exclude-rules:
    - path: _test\.go
      linters:
        - gocyclo
        - funlen
        - errcheck
        - gosec
        - noctx
        - gocognit
        - nestif
        - cyclop
        - goconst
        - errorlint
        - staticcheck
        - stylecheck
        - lll
        - nlreturn
        - unparam
        - gocritic
    - path: \.pb\.go
      linters:
        - goconst
        - gocyclo
        - gosimple
        - govet
        - ineffassign
        - staticcheck
        - stylecheck
        - typecheck
        - unconvert
        - unparam
        - unused
    - path: tests/integration
      linters:
        - goconst
        - gocritic
    - path: internal/errors/errors\.go
      linters:
        - nlreturn
    - path: main\.go
      linters:
        - goconst
    - path: cmd/delete\.go
      linters:
        - nlreturn
    - path: cmd/harness\.go
      linters:
        - goconst
        - nlreturn
    - path: cmd/version\.go
      linters:
        - goconst
    - path: cmd/list\.go
      linters:
        - nlreturn
    - path: cmd/util\.go
      linters:
        - nlreturn
    - path: pkg/env
      linters:
        - goconst
    - path: internal/ui/prompt\.go
      linters:
        - nlreturn
    - path: internal/providers/registry\.go
      linters:
        - nlreturn
    - text: "exported (function|method|type|const)"
      linters:
        - stylecheck
    - text: "commented out"
      linters:
        - stylecheck
    # Functions with legitimately high complexity due to their nature
    - text: "MigrateConfigOnUpdate"
      linters:
        - cyclop
        - funlen
    - text: "GenerateWrapperScript"
      linters:
        - cyclop
        - funlen
    - text: "configureProvider"
      linters:
        - funlen
    - text: "EncryptSecrets"
      linters:
        - funlen
    # Context-aware functions with multiple cancellation checks
    - text: "migrateConfigFile"
      linters:
        - funlen
        - cyclop
    - text: "LoadConfig"
      linters:
        - funlen
        - cyclop
